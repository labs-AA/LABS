pipeline {
    agent { label 'Host1','Host2'
    }
    stages {
        stage('Deploy') {
            steps {
                timeout(time: 1, unit: 'MINUTES') {
                    sh '/bin/hostname'
                }
                timeout(time: 1, unit: 'MINUTES') {
                    sh '/bin/hostname'
                }
            }
        }
    }
}
